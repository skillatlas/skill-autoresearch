---
provider: claude
---

Improve the skill.

Edit `skills/demo/SKILL.md` and increment the integer in `version=<n>` by exactly 1.

Rules:
- Keep the file as a single line in the same `version=<n>` format.
- Do not create or modify any other files.
